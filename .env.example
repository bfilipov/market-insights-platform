# ==========================================
# Market Data Service Configuration
# ==========================================

# Server Configuration
MARKET_DATA_HOST=0.0.0.0
MARKET_DATA_PORT=8001

# Internal Authentication
# Used by API Gateway Service to authenticate requests to internal services
SERVICES_INTERNAL_API_KEY=internal-service-key-market-data

# Provider Configuration
# Sets the default data provider. Options: "coingecko" or "coincap"
MARKET_DATA_DEFAULT_PROVIDER=coingecko

# External Provider URLs
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
COINCAP_BASE_URL=https://rest.coincap.io/v3

# External Provider URLs
COINCAP_API_KEY="your-coincap-api-key" # A free API key should be generated from https://pro.coincap.io/dashboard

# Resilience Configuration
# Timeout in seconds for external API calls
MARKET_DATA_REQUEST_TIMEOUT=10
# Maximum number of retry attempts for transient network errors
MARKET_DATA_MAX_RETRIES=3

# ==========================================
# Api Gateway Service Configuration
# ==========================================

API_GATEWAY_HOST=0.0.0.0
API_GATEWAY_PORT=8000

# Path to the users JSON file (inside the container)
API_KEYS_FILE=/data/users.json
API_GATEWAY_ADMIN_API_KEY=your-admin-api-gateway-key

MARKET_DATA_SERVICE_URL=http://market_data:8001
MARKET_SIGNAL_SERVICE_URL=http://market_signal:8002

API_GATEWAY_BOOTSTRAP_USER_API_KEY=bootstrap-api-key-used-to-seed-the-first-user
API_GATEWAY_BOOTSTRAP_USER_NAME="Local Bootstrap User"
API_GATEWAY_BOOTSTRAP_USER_EMAIL=bootstrap@example.com

LOG_LEVEL=INFO